{
  "name": "puretokens-skill",
  "version": "0.18.2",
  "description": "Installable Pure Tokens skills and skill management tooling.",
  "private": true,
  "type": "module",
  "files": [
    "schemas/",
    "scripts/",
    "skills/",
    "references/",
    "runtime/",
    ".agents/",
    "assets/",
    "README.md",
    "README.zh-CN.md",
    "CHANGELOG.md",
    "CHANGELOG.zh-CN.md"
  ],
  "scripts": {
    "list": "node scripts/list-skills.mjs",
    "validate": "node scripts/validate-skills.mjs && node scripts/sync-skill-model-profiles.mjs --check && node scripts/sync-readme-media-catalog.mjs --check && npm run acceptance:validate && node scripts/executor-build-proof.mjs",
    "acceptance:validate": "node scripts/validate-host-acceptance.mjs",
    "release:validate": "npm run validate && node scripts/validate-release-version.mjs && node scripts/check-media-catalog-freshness.mjs --max-age-days 7 && npm run executor:verify",
    "docs:sync-media-models": "node scripts/sync-skill-model-profiles.mjs --write && node scripts/sync-readme-media-catalog.mjs --write",
    "docs:sync-media-models-from-base-catalog": "node scripts/sync-media-model-catalog-from-service.mjs --write && npm run docs:sync-media-models",
    "docs:sync-media-models-from-service": "npm run docs:sync-media-models-from-base-catalog",
    "test": "node --test",
    "check": "npm run validate && npm run executor:test && npm test",
    "executor:build": "node scripts/build-executor.mjs",
    "executor:test": "cd runtime/executor && go test ./... -count=1 && go vet ./...",
    "release:package": "node scripts/package-platform-releases.mjs && npm run release:verify",
    "release:verify": "node scripts/validate-platform-releases.mjs",
    "executor:verify": "node scripts/executor-build-proof.mjs --rebuild"
  },
  "engines": {
    "node": ">=20"
  }
}
