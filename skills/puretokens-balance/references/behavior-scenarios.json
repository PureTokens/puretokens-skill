{
  "$schema": "https://puretokensx.com/schemas/media-behavior-scenarios.schema.json",
  "schemaVersion": 1,
  "kind": "balance",
  "scenarios": [
    { "id": "balance-account-session-unavailable", "when": "GET https://api.puretokensx.com/api/product/desktop/account/balance is not authenticated or cannot be executed", "then": "report the returned status or error and direct the user to the Pure Tokens client balance view; do not estimate or request credentials" },
    { "id": "balance-host-native-api-executor-unavailable", "when": "the current session does not expose a host-native authenticated HTTPS/API executor", "then": "state that the fixed balance API did not execute and stop. Do not open or control any browser, Switch, Desktop, or graphical interface, request or use Computer Use, or invoke another Skill as a fallback" },
    { "id": "balance-response", "when": "the fixed Pure Tokens balance API returns a balance response", "then": "report only returned fields once; do not retry or infer omitted values" }
  ]
}
