{
  "$schema": "https://puretokensx.com/schemas/media-behavior-scenarios.schema.json",
  "schemaVersion": 1,
  "kind": "update",
  "scenarios": [
    {
      "id": "update-init",
      "when": "the user explicitly asks to initialize or verify the current connection",
      "then": "invoke init once for public identity followed by one authenticated catalog read, sharing one 20-second total deadline without retries; separate credential verification from identity and show only the relevant sanitized result"
    },
    {
      "id": "update-local-connection-failure-stops-host-probing",
      "when": "A command for the bound host returns an active_connection_*, workbuddy_*, or host_credential_adapter_unavailable local failure, including when the user asks to continue generation.",
      "then": "Stop the automatic API flow. Do not invoke connection, init, doctor, models, another Skill, or another --host as a recovery probe. Preserve the original request and any existing task ID; never resubmit automatically. Explain only that this invocation sent no API request. An explicit recheck request or verified new diagnostic evidence permits one relevant check on the same host; changing host requires explicit user selection."
    },
    {
      "id": "update-init-unverified",
      "when": "init cannot resolve a verified credential, cannot reach the fixed API, receives an API rejection, or receives an unconfirmed identity",
      "then": "state that configuration verification is incomplete, show the executor's sanitized status, message, next action, and only a returned non-zero HTTP status, then still show the usage guide; do not modify configuration, claim media availability, or expose host configuration"
    },
    {
      "id": "update-host-unknown",
      "when": "the active host cannot be identified as one of the registered supported hosts",
      "then": "Ask which supported host the user is using; do not guess the host or its configuration/Skill directory."
    },
    {
      "id": "update-terminal-unavailable",
      "when": "no local terminal, PowerShell, exec, or shell execution is available",
      "then": "state that local synchronization cannot run in this session and direct the user to give the official repository installation prompt to a terminal-capable local Agent; do not invent a manual copy command or claim success"
    },
    {
      "id": "update-fresh-source-checkout",
      "when": "an official Skill install or update is requested",
      "then": "Use the official native fetch wrapper, installed or freshly downloaded from the stable release to a local file. Pin the stable manifest version and source commit, verify its selector and current-platform archive, then invoke native sync. No main/source archive fallback, third-party mirror, remote shell pipe or user runtime installation."
    },
    {
      "id": "update-source-acquisition-unavailable",
      "when": "the stable manifest, selector or platform archive cannot be downloaded or the native installer cannot execute",
      "then": "Report the actual failure and stop before installed-file writes. Do not install Git, Node, Python, Go, dependencies or an alternate transport."
    },
    {
      "id": "update-validation-failed",
      "when": "the stable selector or platform archive fails checksum or source validation",
      "then": "report the validation failure and stop before changing installed Skills. Do not run Node, npm packages, generators, docs sync, formatters, repair commands, Git writes, or any other source-mutating command to make the official checkout pass."
    },
    {
      "id": "update-execution-unconfirmed",
      "when": "the host interrupts, times out, or otherwise returns before the installer prints the exact synchronized-version completion receipt",
      "then": "Report synchronization as unconfirmed without automatic retry. On the next explicit request, the official fetch/sync path can recover a managed interrupted transaction."
    },
    {
      "id": "update-unmanaged-conflict",
      "when": "a current target directory is missing the matching managed Skill markers",
      "then": "report the exact conflict and stop before changing any target directory; do not overwrite, delete, rename, or merge it"
    },
    {
      "id": "update-managed-sync",
      "when": "a supported host has no conflicting target directories",
      "then": "Run the official sync once, report the synchronized version and relevant changes, then advise a new host conversation. Do not repeat the full six-Skill version list."
    },
    {
      "id": "update-help-only",
      "when": "the user only asks how to use installed Skills",
      "then": "Read the installed usage guide and answer directly. No init, doctor, credential access, network read or update."
    },
    {
      "id": "update-check-only",
      "when": "the user asks whether an update exists",
      "then": "Run fetch check-update for the current host. It reads the stable manifest and verified selector to compare the installed version without synchronization or init. A matching version alone does not prove installed integrity."
    },
    {
      "id": "update-doctor",
      "when": "the user explicitly requests installation or connection diagnostics",
      "then": "Invoke the installed native doctor once with the current host. Report known-directory version/duplicate findings and separate public identity, credential checks, checksum status and unverified host attachment delivery. Do not scan unrelated paths or auto-fix."
    },
    {
      "id": "update-ownership-conflict",
      "when": "a same-name directory lacks verified ownership or has added, modified, missing or symlinked entries",
      "then": "stop replacement and preserve the directory; do not delete files, regenerate ownership markers, or claim synchronization; interrupted recovery retains the backup on the same conflict"
    },
    {
      "id": "old-marked-selector-new-host",
      "when": "a sibling installer carries the old locate marker but does not support the requested host",
      "then": "download and checksum-verify the directory selector from the same stable release; never reuse the sibling based only on its marker"
    },
    {
      "id": "same-version-verified",
      "when": "install or update finds the selected stable version already installed",
      "then": "Verify the selected release executor checksum and all seven managed inventories. Only an intact installation with no unresolved transaction returns already current, without archive download, file writes or init. Missing or modified files stop without automatic repair."
    },
    {
      "id": "install-host-launch-failure",
      "when": "official installation cannot launch the executor, capture output or finish within its timeout",
      "then": "Stop and report completed stages and the sanitized failure. Do not create probes, shims, Python patches, modify official scripts, switch launch mechanisms, run doctor repeatedly or resubmit installation automatically. Required host approvals remain required."
    }
  ]
}
