{
  "$schema": "https://puretokensx.com/schemas/media-behavior-scenarios.schema.json",
  "schemaVersion": 1,
  "kind": "update",
  "scenarios": [
    { "id": "update-host-unknown", "when": "the active host cannot be identified as Claude Code, Codex, WorkBuddy, Gemini CLI, Grok Build, OpenCode, or Trae", "then": "ask the user which supported host they use and do not modify files" },
    { "id": "update-terminal-unavailable", "when": "no local terminal, PowerShell, exec, or shell execution is available", "then": "state that automatic local upgrade cannot run and provide the native official copyable command for the identified host; do not claim success" },
    { "id": "update-native-installer-bootstrap", "when": "an existing older official Skill installation has no native platform installer", "then": "download the matching official Shell or PowerShell installer into a new private temporary directory and execute that local file. Do not pipe remote content into a shell, use Node, npm, Git, or install dependencies." },
    { "id": "update-validation-failed", "when": "the native platform installer cannot download or statically validate the compact official install payload before sync", "then": "report the validation failure and stop before changing installed Skills. Do not install Node, npm packages, Git, generators, docs sync, formatters, repair commands, Git writes, or any other source-mutating command to make the official source pass." },
    { "id": "update-execution-unconfirmed", "when": "the host interrupts, times out, or otherwise returns before the installer prints the exact synchronized-version completion receipt", "then": "state that synchronization is unconfirmed and do not claim an update, infer a version, automatically retry, or create a background task. On the user's next explicit request, run one fresh official sync." },
    { "id": "update-unmanaged-conflict", "when": "a current or retired target directory is missing the matching managed Skill markers", "then": "report the exact conflict and stop before changing any target directory; do not overwrite, delete, rename, or merge it" },
    { "id": "update-managed-retired-skill", "when": "a verified retired managed Skill directory or an older hidden retired backup is present in the target root", "then": "after all current official Skills install or upgrade successfully, delete that verified retired directory so the host cannot continue discovering it; never delete an unmanaged directory" },
    { "id": "update-codex-legacy-plugin", "when": "the target is Codex's ~/.agents/skills and the official Codex plugin list contains the exact legacy puretokens-media plugin", "then": "after current Skills synchronize successfully, remove only that exact plugin through the official Codex plugin interface; never read plugin configuration or remove another plugin" },
    { "id": "update-codex-legacy-plugin-unavailable", "when": "the Codex CLI cannot inspect or remove the legacy plugin, including workspace-managed plugin cases", "then": "report that legacy Puretokens Media must be removed in Codex Plugins or by the workspace administrator; do not claim removal succeeded" },
    { "id": "update-managed-sync", "when": "a supported host has no conflicting target directories", "then": "run the official sync command once, report every installed or upgraded Skill and the synchronized official version, then tell the user to start a new host conversation" }
  ]
}
