{
  "$schema": "https://puretokensx.com/schemas/media-behavior-scenarios.schema.json",
  "schemaVersion": 1,
  "kind": "update",
  "scenarios": [
    {
      "id": "update-init",
      "when": "the user explicitly asks to initialize or verify the current connection",
      "then": "invoke init once for public identity followed by one authenticated catalog read; separate credential verification from identity and show only the relevant sanitized result"
    },
    {
      "id": "update-init-unverified",
      "when": "init cannot resolve a verified credential, cannot reach the fixed API, receives an API rejection, or receives an unconfirmed identity",
      "then": "state that configuration verification is incomplete, show the executor's sanitized status, message, next action, and only a returned non-zero HTTP status, then still show the usage guide; do not modify configuration, claim media availability, or expose host configuration"
    },
    {
      "id": "update-host-unknown",
      "when": "the active host cannot be identified as Claude Code, Codex, WorkBuddy, Gemini CLI, Grok Build, OpenCode, or Trae",
      "then": "Ask which supported host the user is using; do not guess the host or its configuration/Skill directory."
    },
    {
      "id": "update-terminal-unavailable",
      "when": "no local terminal, PowerShell, exec, or shell execution is available",
      "then": "state that local synchronization cannot run in this session and direct the user to give the official repository installation prompt to a terminal-capable local Agent; do not invent a manual copy command or claim success"
    },
    {
      "id": "update-fresh-source-checkout",
      "when": "an official Skill install or update is requested",
      "then": "Use the official native fetch wrapper, installed or freshly downloaded to a local file. It pins official main to one commit and version, selects matching checksum-verified platform assets or retrieves that exact official source archive, then invokes its sync. No third-party mirror, remote shell pipe or user runtime installation."
    },
    {
      "id": "update-source-acquisition-unavailable",
      "when": "the official pinned package/source cannot be downloaded or the native installer cannot execute",
      "then": "Report the actual failure and stop before installed-file writes. Do not install Git, Node, Python, Go, dependencies or an alternate transport."
    },
    {
      "id": "update-validation-failed",
      "when": "the pinned official package or source fails checksum or source validation",
      "then": "report the validation failure and stop before changing installed Skills. Do not run Node, npm packages, generators, docs sync, formatters, repair commands, Git writes, or any other source-mutating command to make the official checkout pass."
    },
    {
      "id": "update-execution-unconfirmed",
      "when": "the host interrupts, times out, or otherwise returns before the installer prints the exact synchronized-version completion receipt",
      "then": "Report synchronization as unconfirmed without automatic retry. On the next explicit request, the official fetch/sync path can recover a managed interrupted transaction."
    },
    {
      "id": "update-unmanaged-conflict",
      "when": "a current or retired target directory is missing the matching managed Skill markers",
      "then": "report the exact conflict and stop before changing any target directory; do not overwrite, delete, rename, or merge it"
    },
    {
      "id": "update-managed-retired-skill",
      "when": "a verified retired managed Skill directory or an older hidden retired backup is present in the target root",
      "then": "after all current official Skills install or upgrade successfully, delete that verified retired directory so the host cannot continue discovering it; never delete an unmanaged directory"
    },
    {
      "id": "update-managed-executor-and-legacy-node-runtime",
      "when": "the target is synchronized",
      "then": "install exactly one checksum-verified platform native executor in .puretokens-executor. If the target also contains .puretokens-runtime, remove it only when both old official files are present and runtime.json identifies puretokens-direct-api-runtime; otherwise leave it untouched."
    },
    {
      "id": "update-codex-legacy-plugin",
      "when": "the target is Codex's ~/.agents/skills and the official Codex plugin list contains the exact legacy puretokens-media plugin",
      "then": "before synchronizing Skills, remove only the exact listed plugin selector through the official Codex plugin interface, then list plugins again and require that puretokens-media is absent; never read plugin configuration or remove another plugin"
    },
    {
      "id": "update-codex-legacy-plugin-unavailable",
      "when": "the Codex CLI cannot inspect, remove, or verify removal of the legacy plugin, including workspace-managed plugin cases",
      "then": "If plugin inspection is unavailable, report that limitation and continue normal installation. If a legacy plugin was actually discovered but cannot be removed and verified, stop migration and guide its removal in Codex Plugins."
    },
    {
      "id": "update-codex-legacy-plugin-restart",
      "when": "the exact legacy puretokens-media plugin was removed and verified",
      "then": "require a full Codex application restart, then a brand-new conversation before testing; an existing conversation can retain the already-loaded legacy Skill instructions"
    },
    {
      "id": "update-managed-sync",
      "when": "a supported host has no conflicting target directories",
      "then": "Run the official sync once, report the synchronized version and relevant changes, then advise a new host conversation. Do not repeat the full six-Skill version list."
    },
    {
      "id": "update-help-only",
      "when": "the user only asks how to use installed Skills",
      "then": "Read the installed usage guide and answer directly. No init, doctor, credential access, network read or update."
    },
    {
      "id": "update-check-only",
      "when": "the user asks whether an update exists",
      "then": "Run fetch check-update for the current host. It reads the pinned official version and managed installed version without synchronization or init. Report a difference without claiming that files changed."
    },
    {
      "id": "update-doctor",
      "when": "the user explicitly requests installation or connection diagnostics",
      "then": "Invoke the installed native doctor once with the current host. Report known-directory version/duplicate findings and separate public identity, credential checks, checksum status and unverified host attachment delivery. Do not scan unrelated paths or auto-fix."
    }
  ]
}
