# Pure Tokens Skill 使用须知

已安装的原生执行器通过当前宿主文档列出的 Pure Tokens 连接记录完成固定 API 请求，不需要粘贴 Key，也不需要 Node、Python、Go 或 Desktop。只问用法时不会检查连接或访问网络。

常用示例：

- `生成一张日落时分的山间湖泊图片。`
- `用 seedream-5.0-pro 生成同一主题的 3 张图片变体。`
- `生成一段 5 秒、电影感的纸船漂过河面的短视频。`
- `用当前附件作为首帧生成视频。`
- `列出支持首帧、720p 的视频模型。`
- `只检查这些生图参数，不生成。`
- `继续这个任务记录，取出尚未交付的图片。`
- `查询当前 Pure Tokens 余额。`
- `检查是否有 Skill 更新。`／`更新 Pure Tokens Skills。`
- `诊断当前宿主的 Skill 安装和连接。`

普通生成只读模型索引与选中 profile，不先查余额、目录或 init。实时目录仅用于明确查询、所需字段／操作缺口或拒绝诊断；未声明的可选参数不会提交。物理尺寸只能用作比例指导，不能直接作为 API 像素尺寸。

任务接受后先返回 ID；超时可以明确继续同一个任务，不能自动重提。可选任务记录留在用户／工作区明确位置，保存原任务和交付进度，不存 prompt、凭据、参考 URL 或媒体字节。实际交付附件后才记录已交付；下载成功不等于宿主交付成功。

本地附件只随声明的 multipart 请求发送，不额外上传、不转存或改成纯文本。只有实际原生图片／视频附件才是完成交付。init 成功不保证模型权限、余额、媒体创建或宿主交付；doctor 也不会把未验证的校验／交付写成成功。余额复用现有 API Key 查询：不限额 Key 显示账户钱包余额，限额 Key 显示该 Key 的剩余额度，单位 USD，不含订阅额度。精确报价、服务端幂等和无 ID 任务查找尚未提供。
