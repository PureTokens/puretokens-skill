interface:
  display_name: "Pure Tokens Media & Balance"
  short_description: "Check a host-exposed Pure Tokens balance, or generate images and videos with available Pure Tokens models."
  default_prompt: "Use $puretokens_media to check my Pure Tokens balance or generate an image or video through Pure Tokens."

policy:
  allow_implicit_invocation: true
