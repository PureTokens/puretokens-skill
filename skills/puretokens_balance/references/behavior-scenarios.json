{
  "$schema": "https://puretokensx.com/schemas/media-behavior-scenarios.schema.json",
  "schemaVersion": 1,
  "kind": "balance",
  "scenarios": [
    { "id": "balance-capability-unavailable", "when": "the host does not expose an authenticated read-only balance capability", "then": "tell the user to use the current connection's client entry; do not estimate or request credentials" },
    { "id": "balance-response", "when": "the host returns a balance response", "then": "report only returned fields once; do not retry or infer omitted values" }
  ]
}
