interface:
  display_name: "Pure Tokens Connection"
  short_description: "Check whether the current API endpoint identifies itself as Pure Tokens API."
  default_prompt: "Use $puretokens_connection to check whether my current connection identifies itself as Pure Tokens API."
