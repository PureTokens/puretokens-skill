{
  "$schema": "https://puretokensx.com/schemas/media-behavior-scenarios.schema.json",
  "schemaVersion": 1,
  "kind": "connection",
  "scenarios": [
    { "id": "connection-identity-confirmed", "when": "GET https://api.puretokensx.com/v1 returns status ok, name Pure Tokens API, and base_url /v1", "then": "state that the fixed API used by the Skill identifies itself as Pure Tokens API" },
    { "id": "connection-identity-unconfirmed", "when": "GET https://api.puretokensx.com/v1 returns unreadable JSON, missing fields, or values other than the declared Pure Tokens API identity", "then": "state that the fixed Pure Tokens API did not return a confirmable identity and do not assert that the user's other connection belongs to another service" },
    { "id": "connection-identity-unavailable", "when": "the direct GET https://api.puretokensx.com/v1 request fails", "then": "state that the fixed API could not be checked, report the returned result when available, and do not try another endpoint or request credentials" },
    { "id": "connection-base-url-request", "when": "the user asks for their actual configured Base URL, API key, provider label, or host configuration", "then": "state that the Skill does not read host configuration and direct the user to their client or CC Switch settings; clarify that media Skills themselves use the fixed Pure Tokens API origin" },
    { "id": "connection-identity-assurance", "when": "the user needs proof stronger than the API endpoint public identity declaration", "then": "explain that the existing check validates the endpoint declaration only and does not claim cryptographic anti-spoof verification" }
  ]
}
