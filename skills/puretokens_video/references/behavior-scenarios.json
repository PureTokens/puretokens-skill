{
  "$schema": "https://puretokensx.com/schemas/media-behavior-scenarios.schema.json",
  "schemaVersion": 1,
  "kind": "video",
  "scenarios": [
    { "id": "video-model-alias-ambiguous", "when": "an alias resolves to multiple exact model IDs", "then": "ask the user to choose an exact ID; do not submit" },
    { "id": "video-model-unavailable", "when": "the authenticated catalog lacks the exact video ID or capability", "then": "state that the model is unavailable or unauthorized and list catalog candidates; do not submit" },
    { "id": "video-model-parameter-profile-unavailable", "when": "the authenticated live catalog has no input schema for a requested duration, aspect ratio, resolution, size, or other optional parameter", "then": "explain that the model parameter profile is not published, ask the user to remove that option or choose a model with an explicit profile, and do not submit" },
    { "id": "video-model-parameter-unsupported", "when": "a requested optional parameter is absent from the authenticated live input schema or its value violates the declared enum or range", "then": "list the profile-supported values and ask the user to choose one; do not submit" },
    { "id": "video-execution-unavailable", "when": "the current connection cannot execute the request or deliver native video bytes", "then": "stop before submission and ask the user to check the existing connection configuration" },
    { "id": "video-parameter-unsupported", "when": "a requested duration, aspect ratio, resolution, or size is rejected or unsupported", "then": "report the unsupported parameter and ask the user for a supported value; do not alter it silently" },
    { "id": "video-resolution-mode-unsupported", "when": "a requested resolution is outside the exact live profile's resolution_by_mode set for text, image, or reference generation, or the request cannot map its operation to a declared mode", "then": "list the applicable mode's declared resolution values when available, ask the user to choose one or remove the unresolved parameter, and do not submit" },
    { "id": "video-image-operation-profile-unavailable", "when": "the user attaches an image for image-to-video or reference-image video and the exact authenticated model lacks the matching input_schema operation", "then": "state that the selected model has not published that native-attachment operation and do not submit or switch models" },
    { "id": "video-media-operation-profile-unavailable", "when": "the user attaches reference video, reference audio, or a source video for editing and the exact authenticated model lacks that complete input_schema operation", "then": "state that the selected model has not published the native-attachment operation and do not submit or switch models" },
    { "id": "video-image-transport-unavailable", "when": "the user media cannot be delivered in one of the selected profile's declared transports", "then": "list the declared transports, explain why the current attachment/reference is unavailable, and do not create a URL, use a separate upload path, or downgrade it" },
    { "id": "video-public-url-reference-profile-unavailable", "when": "the user provides a public image, video, or audio URL, or a file/voice ID, but the exact authenticated profile lacks a matching property and declared transport", "then": "state that the selected model has not published a compatible public-reference field, list any declared fields, and do not submit or switch models" },
    { "id": "video-public-url-reference-invalid", "when": "the user provides a URL reference that is not a public HTTPS URL", "then": "explain that the selected profile requires a public HTTPS URL and ask the user to provide one; do not submit" },
    { "id": "video-public-url-reference-ambiguous", "when": "more than one exact authenticated public-reference field could represent the user's URL or declared ID", "then": "list the declared fields and ask the user to choose the intended input mode; do not guess or submit" },
    { "id": "video-prompt-requirement", "when": "a request omits prompt without the exact authenticated profile declaring that the selected single-reference mode permits omission", "then": "explain that prompt is required for the selected mode and ask the user to provide it; do not submit" },
    { "id": "video-image-r2-staging", "when": "the host can send the user's current multipart image, video, or audio attachment through the declared Videos API operation", "then": "send it only with that single Videos API request; Pure Tokens stages the attachment internally to short-lived R2, waits for a readable public HTTPS URL, maps it to the provider field, and never exposes that URL to the user" },
    { "id": "video-image-count-unsupported", "when": "the requested media/reference count violates the selected profile's declared minItems or maxItems", "then": "state the allowed count and do not submit" },
    { "id": "video-media-combination-unsupported", "when": "the user attaches more than one media type but the selected profile does not declare that exact combined operation", "then": "explain that only one declared media operation can be submitted, ask the user to retain one supported input, and do not split, drop, or submit attachments" },
    { "id": "video-input-media-unsupported", "when": "the user asks for an undeclared media operation or gives a media type/field that does not match the selected profile", "then": "state that the selected model profile does not support that media operation and do not upload media" },
    { "id": "video-task-pending", "when": "the same task is pending or running", "then": "report the current state and continue reading only that task" },
    { "id": "video-task-poll-delay", "when": "the same task is pending or running", "then": "honor a valid positive Retry-After delay when returned; otherwise wait 5, 10, 20, 40, then 60 seconds, with at most seven same-task status reads during a submission or explicit same-task continuation turn" },
    { "id": "video-task-poll-resource-bound", "when": "a task is pending or running", "then": "keep at most one same-task status request in flight, never create a background timer or queue, and stop automatic polling after a rate limit, 5xx, transport error, or timeout" },
    { "id": "video-task-polling-deadline", "when": "300 seconds have elapsed since a task submission response and the task is still pending or running", "then": "report the current state, stop automatic polling, and require an explicit user request to continue the same task; do not resubmit" },
    { "id": "video-task-explicit-continuation", "when": "the user explicitly asks to continue querying the returned task_id", "then": "start a new bounded polling window for that same task only; do not submit, inspect, or substitute another task" },
    { "id": "video-task-terminal-failure", "when": "the same task reports failure, cancellation, or expiry", "then": "report the returned failure and require an explicit new request before another submission" },
    { "id": "video-task-timeout-or-unknown", "when": "the status request times out or the submission outcome is unknown", "then": "report the uncertainty, offer to continue querying the same task if an ID exists, and never auto-resubmit" },
    { "id": "video-content-delivery-failure", "when": "a task is complete but native content cannot be delivered", "then": "report the delivery failure and offer only a same-task content retry; do not claim success" },
    { "id": "video-content-resource-bound", "when": "a task is complete", "then": "read at most one native video content response at a time, hand it off before any explicit same-task retry, and never prefetch or re-fetch delivered content" }
  ]
}
