{
  "$schema": "https://puretokensx.com/schemas/media-behavior-scenarios.schema.json",
  "schemaVersion": 1,
  "kind": "models",
  "scenarios": [
    {
      "id": "models-catalog-unavailable",
      "when": "GET https://api.puretokensx.com/v1/media/models fails",
      "then": "state that the fixed Pure Tokens API did not return model data, report the returned result when available, and do not call another endpoint or request credentials"
    },
    {
      "id": "models-local-connection-failure-stops-host-probing",
      "when": "A command for the bound host returns an active_connection_*, workbuddy_*, or host_credential_adapter_unavailable local failure, including when the user asks to continue generation.",
      "then": "Stop the automatic API flow. Do not invoke connection, init, doctor, models, another Skill, or another --host as a recovery probe. Preserve the original request and any existing task ID; never resubmit automatically. Explain only that this invocation sent no API request. An explicit recheck request or verified new diagnostic evidence permits one relevant check on the same host; changing host requires explicit user selection."
    },
    {
      "id": "models-direct-request-start",
      "when": "a model-catalog read is requested",
      "then": "invoke the installed managed native executor with the current host ID for the fixed catalog GET; do not look for a separate model interface"
    },
    {
      "id": "models-catalog-empty",
      "when": "the authenticated catalog returns no media models",
      "then": "Explain that the requested model data was not returned. Offer the actually returned models. Absence does not prove a key or group configuration error. If access was expected, suggest checking model permissions in the user’s own settings or contacting Pure Tokens support. Do not guess groups, replace the connection, substitute models, retry automatically or use a static catalog as live evidence."
    },
    {
      "id": "models-exact-id-unavailable",
      "when": "the user asks for an exact model ID absent from the authenticated catalog",
      "then": "Explain that the requested model data was not returned. Offer the actually returned models. Absence does not prove a key or group configuration error. If access was expected, suggest checking model permissions in the user’s own settings or contacting Pure Tokens support. Do not guess groups, replace the connection, substitute models, retry automatically or use a static catalog as live evidence."
    },
    {
      "id": "models-capability-filter-empty",
      "when": "no catalog model explicitly declares the requested image, video, or media operation capability",
      "then": "state that no current model declared that capability and do not recommend a model by name similarity"
    },
    {
      "id": "models-parameter-profile-absent",
      "when": "a returned model has no input_schema for a requested optional parameter",
      "then": "report the model without claiming support for that parameter and explain that the directory did not publish the parameter profile"
    },
    {
      "id": "models-operation-profile-absent",
      "when": "a returned model has no matching input_schema.operations entry",
      "then": "report that the model did not publish the requested media operation and do not infer it from a general capability label"
    },
    {
      "id": "models-requirement-ambiguous",
      "when": "the user asks for the best model without a requirement represented by declared capability, parameter, or operation metadata",
      "then": "ask for an explicit compatible constraint or show the complete authenticated catalog; do not rank unverified quality, price, or speed"
    },
    {
      "id": "models-catalog-response",
      "when": "the fixed Pure Tokens API returns authenticated model data",
      "then": "display only returned exact IDs, declared capabilities, properties, non-input constraints, and operations in a user-readable grouped format, then stop without submitting media work"
    }
  ]
}
