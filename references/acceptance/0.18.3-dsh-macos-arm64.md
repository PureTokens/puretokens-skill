# DSH macOS ARM64 0.18.3 acceptance observation

Initial checks on 2026-09-16 verified installation, a fresh native Harness
headless invocation and authenticated read-only API access. Subsequent Desktop
observations through 2026-09-17 are recorded below. An earlier Desktop
submission stopped before a server job or supplier call. A later image edit
completed with a recorded native attachment handoff, but visual correctness
and actual opening remain unverified; no full media acceptance case passed.

## Environment

- Host: DSH Desktop 0.9.0, bundled Harness 0.1.5-rc.2.
- Execution: local bundled native headless profile, not a desktop UI session.
- OS: macOS 27.0, native arm64.
- Shell: zsh 5.9; installer executed by system sh.
- Candidate: 0.18.3, source commit
  `a10729771be16c9936ebcbba7fd03b94a6415088`.
- Package: verified `darwin-arm64` archive from CI run `35102831193`.
- Executor SHA-256:
  `49bbd41b4abeff5e401c48b46a26ce52e9db8b42b37f22a582c02f8f8e0eea63`.
- Embedded executor source SHA-256:
  `77dd38548fe20bd847d1ea9859fa10e2a8cbf3a4eac3149c3acc57475e7ec25c`.

## Observations

1. The native installer synchronized all six Skills and the platform executor
   into the documented default DSH Harness Skill directory. There was no
   prior managed installation in that directory. Installation exited 0 in
   4,645 ms, including automatic initialization.
2. Automatic init ran once and confirmed the fixed API identity and credential
   authentication. This covered the public identity and authenticated media
   catalog reads; they were not repeated as separate connection/models probes.
3. All seven managed inventories and the release-pinned executor checksum
   subsequently passed `verify-installed`, without another init or file writes.
   The installed executable also reported the expected version/build identity.
4. The application's bundled native Harness entrypoint ran a fresh headless
   help-only session using the documented Desktop Harness home. It reported
   loading `puretokens-update` from the installed DSH directory, version
   0.18.3, and the usage guide. The process exited 0 in 30,870 ms.
   No replacement runtime, custom transport, desktop automation or
   authentication override was added. The native launcher initialized its
   standard headless profile; the agent did not read or change connection
   configuration.
5. One installed native `balance --host dsh-desktop` invocation exited 0 in
   1,829 ms. The receipt confirmed an authenticated request and HTTP 200, with
   a numeric remaining amount under `account_wallet` in USD. The amount and
   account details are intentionally absent from this evidence.
6. The headless session reported no native attachment-handoff capability.
   The installed headless driver's implementation prints final assistant text
   and mounts no Host, HTTP or browser plugins. This is insufficient evidence
   for actual attachment presentation, opening images or playing video in the
   Desktop client.

## Acceptance Status

`installation` and `authenticatedAPI` passed only in the recorded local native
Harness environment. The automated headless response is not visual evidence
of the Desktop conversation, and does not establish other architectures,
profiles, installation variants or client versions.

The remaining seven cases are pending: image generation, uploaded-image
editing, generated-image editing, multiple-image generation, video generation,
same-task recovery and native attachment delivery. No paid media submission was
made because the available headless session could not complete native handoff.
Do not replace this requirement with a path, stdout, a Codex attachment, a
fabricated delivery event or a different transport.

Continue in a real DSH Desktop conversation with the installed 0.18.3 Skills
and the same host binding. Capture fresh-session loading and actual attachment
handoff there before treating that execution surface as accepted. Image opening,
video playback and cross-conversation recovery require their own observations;
this headless limitation does not establish that the Desktop client lacks
those capabilities.

## Desktop Attempt At 22:15

The user supplied a screenshot of a real Desktop generation attempt on
2026-09-16. A read-only inspection of the one matching local session correlated
the visible diagnostic identifier without exporting the session, prompts,
connection fields, request headers, account information or raw response text.

- The turn ran from 22:15:38.522 to 22:15:46.033 Asia/Shanghai, lasting 7,511 ms.
- The request advertised the native `skill` tool and included
  `puretokens-image` in the Skill catalog. This proves discovery, not invocation
  or the version of instructions loaded by that Desktop turn.
- The log contains one assistant message with a text block. Its recorded
  model stop reason is `stop`; the turn-end reason is `completed`.
- No tool execution, Skill load, native executor call or attachment-delivery
  event is recorded in the turn. The requested media workflow therefore never
  reached submission in this session.
- The assistant text reports a provider failure, but it arrived in the ordinary
  assistant-content path rather than the client's structured error path.
  The installed adapter maps a nonempty `stop` to a normal response, and the
  agent loop completes a text-only response with no tool calls.

This is a blocked Desktop acceptance attempt, not successful image generation
and not evidence of an image API rejection. The media cases remain pending.
Do not change the earlier read-only API results into a full-media pass.

The local record alone did not establish an HTTP status, retry history,
billing outcome or server-side cause. The production observations below
supersede those unknowns only where explicitly supported; the assistant
text itself remains untrusted evidence.

## Production Correlation

Read-only production inspection covered 14:15:30-14:16:00 UTC on the same
date. The active gateway's access logs and a read-only database transaction
were projected to allowlisted status, timing, usage and routing facts.
No raw logs, response bodies, account/channel identities or credentials
were exported. The inspected gateway image revision was
`dafdbfa01316624e80e68231fb87c60854629442`.

- Two streaming chat requests for the selected model completed at 22:15:45
  Asia/Shanghai, returning HTTP 200 in 5.364 and 5.422 seconds.
- The latter request's 8,855 prompt tokens, including 1,000 cached tokens,
  and 1,000 completion tokens match the Desktop record's 7,855 uncached
  input, 1,000 cached input, 1,000 output and 9,855 total tokens. Correlation
  uses model, timestamp and usage, not the diagnostic identifier printed
  in the assistant text. That identifier did not match the gateway records.
- The companion request is consistent with the separate title-generation
  request recorded locally. Its exact role is inferred, not proven by an
  end-to-end request identifier.
- Both records report `stream_status.status=ok`, `end_reason=done` and one
  channel attempt. Neither request has a matching error or refund log row;
  the bounded application logs contain no relay, settlement or retry failure.
  This rules out the claimed retries at this gateway, not retries inside
  the upstream service.
- Both have positive wallet-consumption log entries. This confirms recorded
  chat consumption, not image-generation charges. No amount is disclosed,
  and no billing correction was made. It does not establish the accuracy of
  the upstream usage or the assistant text's claimed minimum-billing policy.
- Recorded request conversion remained OpenAI Compatible, without a recorded
  model mapping, system-prompt overwrite or parameter-override audit. Current
  settings for the selected route also show no status-code mapping, parameter
  override, forced response formatting or thinking-to-content conversion;
  those current settings are not a historical configuration snapshot.
- The deployed compatible chat handler, OpenAI stream handler and text
  settlement source match the inspected local implementations. The normal
  response path forwards text, accepts the usage and settles consumption;
  the client then treats nonempty text with `stop` as a completed turn.

The established failure boundary is chat content returned and accounted for
as success before any Skill/tool invocation. It is not an image API rejection.
Available evidence does not distinguish an upstream-generated fallback
message from model-generated error prose, or establish the upstream's
internal failure cause. No original upstream response capture was available
in the inspected records.

Follow up with the chat upstream using the correlated request and time, and
review the associated consumption. Any correction should use a verified
structured failure contract, not classify arbitrary assistant prose as an API
error or patch the media Skill to bypass the result. No user-request replay,
paid probe, configuration edit, client switch or production write was made.

## Subsequent Desktop Help Screenshot

After the requested help-only check, the user supplied a Desktop screenshot
showing two tool calls and a rendered usage-guide summary. The response
reported reading local instructions without initialization, update, upgrade
or media generation. This is positive Desktop UI evidence of tool activity
and a help response, unlike the earlier text-only failed attempt.

The tool-call details are collapsed. The screenshot alone does not establish
the exact tool names, loaded path/version, unchanged chat model, fresh-session
identity or absence of network requests. Its examples are documentation,
not evidence that any media task ran.

Keep all seven media cases pending. Continue with one image submission in
the same Desktop client, observe the initial receipt and same-task delivery,
then verify that the actual image attachment opens. Do not automatically
resubmit after a submission, status or delivery failure. A successful help
response does not resolve the earlier chat failure's cause or billing review.

## Desktop Submission At 07:50 On 2026-09-17

The next user screenshot was correlated with the same local Desktop session.
The observed turn ran from 07:49:35.791 to 07:50:37.149 Asia/Shanghai.
Unlike the previous attempt, this turn actually executed the image workflow:

- Eight tool calls loaded `puretokens-image`, read its selected
  `gpt-image-2` profile and executor guide, maintained a local task list,
  wrote a request, ran the installed executor once and removed the request
  file. The task-list entries are not server jobs or completed media cases.
- The single native `submit --host dsh-desktop` invocation ran from
  07:50:20.180 to 07:50:21.936. Its structured receipt reports executor
  version 0.18.3, one requested image, HTTP 503, `failure_phase=submission`,
  `submission_outcome=unknown` and `next_step=await_user`, without a task ID.
- No second submission, status/content request or attachment delivery is
  recorded. The executor's conservative unknown-submission result and
  no-retry behavior are appropriate given only its sanitized HTTP response.

Read-only active production gateway logs confirm one Images generation
request at 07:50:21, returning HTTP 503 in 6.119 milliseconds. Its correlated
application error is the missing reviewed image-parameter mapping condition,
not a supplier generation timeout. Both gateway and BFF image revisions were
`7538a5c791adf6f3b7bdb2b7a04544bb0df61456`; the relevant inspected source
matches that deployed revision.

The active, non-deleted `gpt-image-2` model has its public input schema but
lacks the private `models.endpoints.$ptp_managed_image_request` projection.
Product-owned image parameter metadata exists separately. The gateway's
`PreparePublicManagedImageRequest` rejects the missing projection before
`queuePublicManagedImage` creates a job, reserves billing or calls a supplier.
The bounded 07:50:15-07:50:30 window contains zero API-owned generation jobs
for this model and zero task rows; the correlated request has zero consumption
or error log rows. Together with the matched rejection path, this establishes
that this media submission did not reach paid generation. Chat-model usage is
separate and is not covered by that conclusion.

An isolated, in-memory invocation of the deployed BFF adapter's
`syncReviewedMediaMetadata` reproduced another relevant defect:
`normalizeReviewedMediaEndpointPatch` preserves the execution requirement
but drops an explicitly supplied `$ptp_managed_image_request` field before
the database update. The diagnostic used a query stub and performed no
database or network writes. The exact historical write which left this
production model without the projection was not established.

A subsequent user-requested cross-check reconfirmed the exact gateway error
and missing active-model projection. The enabled managed-image route's
candidate capabilities contain provider mappings and use the ordinary
reviewed-profile synchronization branch, not the special supplier-neutral
global-profile branch. A second isolated diagnostic loaded a real reviewed
managed `gpt-image-2` profile through the compiler and metadata adapter:
the compiled mapping exists, the persisted mapping is absent, and
`async_managed` survives. All three diagnostic assertions passed, with
in-memory query stubs only. This establishes a reproducible persistence gap;
it does not identify the historical write or prove that correcting it alone
will make supplier generation and attachment delivery pass.

Correction belongs in `PureTokenPlus_web`: retain and validate the reviewed
mapping through persistence, reconcile the public gateway's mapping source
with the selected route's reviewed contract, and backfill the affected
production metadata through the corrected authorized workflow. Generic
model presets must not replace route-specific parameters. Add coverage across
compiler, adapter persistence and gateway admission; a compiler-only test
does not cover the reproduced loss.

All seven media cases remain pending. Do not ask for another paid submission
until the server contract is corrected and its read-only readiness checks pass.
This investigation made no production writes, configuration changes, supplier
calls, billing corrections or request replays, and changed no Web source.

## Local Persistence Correction

Following explicit repair authorization on 2026-09-17, a local correction was
made in `PureTokenPlus_web`, based on revision
`7538a5c791adf6f3b7bdb2b7a04544bb0df61456`.
`normalizeReviewedMediaEndpointPatch` now normalizes and retains the reviewed
managed-image mapping. Missing or malformed managed mappings, or mappings on
non-managed/video endpoints, fail before database access. The existing
gateway reader and client request contract are unchanged.

The new `services/bff/test/managed-image-metadata-sync.test.mjs` exercises real
reviewed profiles through compilation and the production adapter with an
in-memory SQL store. Before the fix it reported 16 failures and 3 passes;
after the fix all 19 checks passed. Coverage includes new and stale mappings,
semantic and pixel formats, route-specific edits, invalid-input no-write
behavior and keeping private mappings out of the public schema.

Local verification passed:

- 65 JavaScript checks covering the new regression, metadata adapter,
  reviewed compiler and catalog synchronization.
- 74 additional JavaScript checks covering Admin route saving, admission,
  parameter editing, public catalogs, metadata validation and Pic protocols.
- Targeted Go tests in `controller` and `relay`, including
  `TestPublicManagedImageDurableLifecycle`,
  `TestManagedImageRequestPreservesReviewedPublicParameters` and
  `TestManagedImageProtocolDoesNotDependOnClientHeader`.
- Changed-module syntax and diff whitespace checks.

The sibling-path review found the reviewed compiler and catalog-sync writer
already retain the mapping; the gateway's missing-mapping refusal remains
appropriate. Web's `docs/architecture/media-request-contract.md` now documents
the persistence invariant, regression coverage and required rollout/backfill.
No Skill instructions, executor binary, model selection or client transport
were changed for this server defect.

This is a local code correction, not production recovery or a media acceptance
pass. No commit, push, deployment, production metadata save or paid replay was
performed in this repair. After an authorized release, preserve and save the
affected route's existing reviewed parameters through Admin, read back the
gateway projection, and then resume separately authorized media acceptance.
All seven DSH media cases remain pending.

## Slow Desktop Edit At 09:22 On 2026-09-17

The user's screenshot of an edit running for 8 minutes 52 seconds was matched
to one local Desktop session through its visible request-file name. Only
allowlisted timing, tool categories and structured receipt facts were
extracted. No prompt, attachment path, connection configuration, credential
or raw response is retained here. This was observation of the user's existing
task, not an agent-created media probe or replay.

| Stage | Asia/Shanghai time | Observed duration |
| --- | --- | --- |
| Turn start to first submit | 09:22:16.078-09:31:55.771 | 579,693 ms |
| Single native submit | 09:31:55.771-09:32:33.227 | 37,456 ms |
| Same-task wait | 09:32:46.315-09:32:50.846 | 4,531 ms |
| Content download | 09:34:10.062-09:34:12.178 | 2,116 ms |
| Native presentation recorded | 09:34:22.270 | One attachment |
| Turn completed | 09:34:36.938 | 740,860 ms total |

The screenshot's submit label was a task-list state: the media request had
not yet been issued at the displayed elapsed time. Before submission the
agent made nine serial chat-model calls. Their selected tools loaded the
Skill, updated the task list, read the selected profile, read the executor
guide, updated the task list, copied the reference, wrote the request,
updated the task list and finally submitted. The preparation tools themselves
took approximately 211 ms in total. Three task-list-only model steps consumed
219,673 ms of the pre-submit interval; updating that list is not required by
the image Skill.

A bounded read-only production log query correlated nine chat records by
time window, selected model and exact input-token counts. Their recorded
gateway durations were 4-6 seconds each, with first-response metrics of
1,821-3,635 ms and successful stream status. Local corresponding model steps
lasted approximately 9-106 seconds. This correlation is not an end-to-end
request-ID trace, and gateway duration is not whole-client elapsed time.
The excess latency is outside the measured gateway processing interval;
available evidence does not locate it among client preparation, request
transport, intermediaries and response handling. It must not be described
as proven upstream model thinking or image-generation time.

The single `nano-banana-2` edit was accepted with a task ID. The first wait
observed the same task completed; its exact server completion time is not
known. Download returned `downloaded_awaiting_host_delivery`. The native
`present` tool then succeeded and emitted `deliverables/presented` for one
file, followed by a successful executor `delivered` receipt with
`next_step=done`. The presented local file exists, is 152,621 bytes and has
a JPEG signature. These facts support download and native handoff, not
visual correctness, successful user opening, all editing variants or full
acceptance across the seven pending media cases.

The demonstrated performance problem is excessive serial pre-submit
orchestration together with a large unlocated client-to-gateway timing gap.
Potential follow-up work is to avoid task lists for simple edits, combine
independent reads and preparation, and follow receipt `next_step` without
extra planning turns. Investigate the timing gap separately before claiming
that a Skill-only change fixes it. No functional change, model switch,
configuration edit, interruption, production write or paid replay was made.
This successful Nano edit does not verify deployment or backfill of the
earlier GPT mapping correction.
