# OpenCode macOS ARM64 0.18.2 acceptance observation

Checked on 2026-09-12. This is real local installation and Skill-discovery
evidence only. It is not authenticated API or media acceptance.

## Environment

- Host: OpenCode 1.18.23.
- OS: macOS 26.6.2 (25G83), native arm64.
- Shell: zsh 5.9; installer executed by system sh.
- Candidate: 0.18.2, built from the current worktree.
- Executor platform: `darwin-arm64`.
- Installed executor SHA-256:
  `5607fb02f4bee3c75eca39bb753fa042dc47cb16277de57ecfd3e42f572777c7`.

## Observations

1. The verified platform archive updated the existing OpenCode Skill directory
   and exited 0 in 915 ms.
2. All seven managed inventories passed. Fourteen unrelated sibling entries
   retained their metadata.
3. The installed executor reported version `0.18.2` and embedded build identity
   was verified during packaging and installation.
4. Automatic init was observed once and stopped locally with
   `active_connection_selection_unconfirmed`. No fixed API request or media
   task was created. No connection configuration was changed and no retry was
   attempted.

## Acceptance status

Installation bytes are verified, but the installation case remains pending
until a fresh OpenCode conversation invokes the loaded Skill. Authenticated API,
image/video generation, same-task resume and native attachment handoff remain
pending. The local selection stop does not establish that the user's working
connection is absent or invalid.
