# OpenCode macOS ARM64 0.18.3 acceptance observation

Checked on 2026-09-16. Real-host upgrade, managed-file verification and fresh
Skill invocation passed. Authenticated API and media acceptance remain pending.

## Environment

- Host: OpenCode 1.18.23.
- OS: macOS 27.0, native arm64.
- Shell: zsh 5.9; installer executed by system sh.
- Candidate: 0.18.3, source commit
  `a10729771be16c9936ebcbba7fd03b94a6415088`.
- Package: verified `darwin-arm64` archive from CI run `35102831193`.
- Executor SHA-256:
  `49bbd41b4abeff5e401c48b46a26ce52e9db8b42b37f22a582c02f8f8e0eea63`.
- Embedded executor source SHA-256:
  `77dd38548fe20bd847d1ea9859fa10e2a8cbf3a4eac3149c3acc57475e7ec25c`.

## Observations

1. The native installer upgraded the existing default OpenCode Skill directory
   from 0.18.2 to the verified 0.18.3 archive contents. It exited 0 in 1,368 ms,
   including the local-only initialization stop below.
2. Fourteen unrelated sibling entries retained their metadata. All seven
   managed inventories and the release-pinned executor checksum subsequently
   passed `verify-installed`, without another init or any file writes.
3. The installed executable reported version 0.18.3 and the expected embedded
   build identity.
4. Automatic init ran once and returned `active_connection_not_puretokens`
   before any fixed API request or credential authentication. The API flow
   stopped; no connection configuration was inspected outside the native
   adapter or changed, no other host was tried, and no media task was submitted.
5. A fresh OpenCode conversation then performed a help-only installation check.
   Its native `skill` tool loaded `puretokens-update`; two successful `read`
   operations read the manifest and usage guide. The host reported the default
   OpenCode installation's `puretokens-update` directory, version 0.18.3 and a
   loaded usage guide. The process exited 0 without stderr or any API/executor
   tool call.

## Acceptance Status

`installation` passed for the recorded local upgrade and fresh-session Skill
invocation. This does not establish every clean-install, path, architecture or
execution-mode variant.

The other eight cases remain pending. Connection, models and balance acceptance
requests were not run after the local initialization stop. No image generation,
uploaded-image editing, generated-image editing, multiple-image generation,
video generation, cross-session task recovery or actual media attachment
handoff was attempted.

The diagnostic means the adapter did not recognize the declared connection
under its endpoint rules. It does not prove that the user's working connection
is missing or invalid, or that the service is unavailable. This observation is
not a basis for automatically changing connections, retrying, or substituting
another client's credentials.
