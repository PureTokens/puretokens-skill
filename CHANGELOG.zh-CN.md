<p align="center">
  <a href="./CHANGELOG.md">English</a> · <strong>中文</strong>
</p>

# 更新日志

## 0.14.2 — 2026-09-04

- 将 Images、Videos、Models、Connection 和 Balance 的固定宿主原生 API 路径收紧为唯一执行路径：明确禁止 Computer Use、浏览器或桌面 UI 自动化、打开 Pure Tokens Switch/Desktop，以及回退调用其他生图或生视频 Skill。
- 为缺少宿主原生 API 执行器增加契约和场景校验：在任何请求前以本地 validation 失败停止，不再搜索可见生成界面或请求 Computer Use 权限。
- 当前宿主使用 Pure Tokens 连接时，将 `puretokens-image` 和 `puretokens-video` 声明为普通图片、视频请求的第一选择，优先于通用 `imagegen`、Imagen 或视频 Skill；契约也明确了宿主侧元数据边界：若宿主忽略已安装 Skill 优先级，Skill 本身无法在运行时强制改变宿主选择。

## 0.14.1 — 2026-09-04

- 降低普通媒体请求的上下文负担：生图和生视频先读取紧凑模型索引，再只读取被选中模型的 profile；完整异常场景只在命中对应特殊路径时读取。
- 将单个大模型选择文件替换为生成的逐模型 profile，同时继续保持一个受控基础模型目录和发布新鲜度校验。
- 使用当前仅源码安装器和 Skill 重新构建一次性旧版迁移归档；其中不再包含 Node 运行器、直连 API 执行器或已废弃的单体模型选择文件。

## 0.14.0 — 2026-09-04

- 将所有受支持宿主的直连路径从自定义 Node 运行器切换为宿主原生的认证 HTTPS/API 能力。图片、视频、模型、连接和余额均固定请求 `https://api.puretokensx.com`，不使用 MCP、代理、sidecar 或本地执行器。
- 明确第三方 CC Switch 和用户手动配置宿主的连接契约：仅可为固定请求在内存中使用当前匹配凭据；Skill 不检查 provider 标签，也不暴露连接配置。
- 更新安装和升级流程：不再安装 API 运行器；只有精确识别为旧官方 Node 运行器时才删除。同步更新用户文档、契约、场景和校验。

## 0.13.26 — 2026-09-04

- 移除自定义临时安装载荷 ZIP 及其下载链路。安装和更新现在先获取本仓库 `main` 的最新官方本地检出，再从该检出运行仅负责同步的原生脚本；脚本自身不再下载任何内容。仅保留一个未在用户安装说明中链接的旧版迁移归档，用于已发布的 0.13.25 更新器：它的升级路径已硬编码为这个文件，否则会无法完成迁移。

## 0.13.25 — 2026-09-04

- 修复 Codex 等受管宿主在真正发出 Images/Videos API 请求前可能卡住的问题：所有受管运行器 POST 统一只接受有界、规范的 Base64 请求体参数；标准输入请求体会立即被拒绝，不再等待宿主可能永远不关闭的 EOF。
- 增加运行器 90 秒总期限以及脱敏的结构化 JSON 失败回执和执行阶段。Skill 现在能区分“本地校验阶段未提交”和“提交是否被接受未知”，且不会重复创建付费任务。
- 明确运行器启动前被宿主策略拦截的处理：它是本地 `validation` 失败，不会产生 API 请求、任务 ID、HTTP 响应或计费结论。图片和视频 Skill 只会引导用户在可批准外部网络执行的会话中重试。

## 0.13.24 — 2026-09-03

- 修复旧 Codex 插件迁移漏洞：安装器现在按已列出的精确 `puretokens-media` 插件 selector 卸载，并再次列出验证其已不存在；无法证明移除成功时停止且不输出同步成功回执。确认移除后明确要求完全重启 Codex，因为已打开对话会保留已加载的旧 Skill 指令。

## 0.13.23 — 2026-09-03

- 将 Agent 安装提示收敛为仓库地址；所有引导细节移入一次下载的官方安装包。原生安装器现在接收宿主 ID，自行推导标准 Skill 目录、校验解压安装包并同步，不再第二次下载安装包。

## 0.13.22 — 2026-09-03

- 将 Agent 安装引导改为“单次下载紧凑载荷”：解压后的最新原生安装器显式接收已校验源目录，不再先下载 runtime 安装器、再第二次下载载荷。

## 0.13.21 — 2026-09-03

- 将可复制的 Agent 安装提示明确为：更新时先从本仓库 `main/runtime` 引导下载最新平台安装器，不得让已安装的旧更新器直接运行，解决旧更新器无法自我更新的循环。

## 0.13.20 — 2026-09-03

- 提升短宿主命令窗口中的原生 Skill 更新可靠性：紧凑载荷先通过 GitHub 官方 API raw-content endpoint 尝试 20 秒；仅当该路径失败时，才通过 GitHub raw-content 路径再尝试一次、最多 20 秒。每次更新还会先引导下载最新平台安装器，避免旧安装器持续使用过期网络路径。静态载荷校验成功前，绝不使用第三方镜像、反复重试来源或开始同步目标目录。

## 0.13.19 — 2026-09-03

- 收紧直连凭据绑定：运行器只会在宿主已记录的配置明确指向固定 Pure Tokens endpoint 时使用该 Key；绝不会将本地、客户端受管或其他连接的 Key 发送给 Pure Tokens API。配置 URL 仅用于确认凭据来源；余额、连接、模型、Images 和 Videos 请求仍全部使用固定公开 API URL。

## 0.13.17 — 2026-09-03

- 针对宿主命令执行窗口较短的问题优化 `puretokens-update`：原生安装器不再下载完整仓库 ZIP，而是下载不含文档和品牌资源、仍会静态校验的紧凑官方安装载荷；单次载荷下载硬性限制为 45 秒。宿主超时或中断现在只会标记为“未确认同步完成”，绝不会谎称更新成功或猜测版本。

## 0.13.16 — 2026-09-03

- 为 `puretokens-update` 增加仅限 Codex 的旧插件迁移：同步 Codex 的 `~/.agents/skills` 时，会在 Skill 同步成功后只通过官方 Codex 插件接口检查并移除精确匹配且已安装的 `puretokens-media`。如果无法检查或卸载（包括工作区受管场景），会明确提示用户到 Plugins 操作或联系管理员，绝不虚报成功。

## 0.13.15 — 2026-09-03

- `puretokens-update` 成功完成后会在安装、升级和旧 Skill 清理结果后，明确报告本次同步的官方 Skill 版本号。

## 0.13.14 — 2026-09-03

- 调整 `puretokens-update` 和两个原生安装器：当前 Skill 全部同步成功后，会删除已验证的旧受管 Skill 目录，并清理早期安装器遗留的隐藏旧版本备份，避免宿主继续发现废弃的 `puretokens_media` 指令。任何非受管的当前、旧版或隐藏备份目录仍会在预检阶段触发冲突，绝不会被删除。

## 0.13.13 — 2026-09-03

- 面向用户的 Skill 更新链路改为官方原生平台安装器：macOS/Linux 使用 Shell，Windows 使用 PowerShell。安装和升级会在同步前下载并静态校验官方 `main` 载荷，用户无需安装 Node、npm、Git、包管理器或依赖。

## 0.13.11 — 2026-09-03

- 当前请求中的本地视觉参考图现在会直接使用所选模型已声明的 multipart `image_edit` operation，同时在提示词中保留用户的参考图意图；绝不上传、转存、伪造 URL，也不会把无法传输的附件静默改成纯文生图。
- 落实图片尺寸表达约束：`width`、`height` 等成对字段必须完整；用户同时给出多种尺寸表达时，只提交资料声明优先级最高的一种。
- 明确视频 operation 的意图映射：首帧请求使用 `image_to_video` 及其声明字段（包括 Wan 的 `first_frame_image`）；已声明 `generate_audio` 时才将“带声音/静音”映射为布尔参数。仅附图片但未说明首帧或参考图角色时，会在计费前询问。

## 0.13.10 — 2026-09-03

- 已将安装载荷中的媒体 profile 与当前 Pure Tokens 基础模型目录同步。`gpt-image-2`、`seedream-5.0-pro`、`nano-banana-2`、`nano-banana-2-lite`、`nano-banana-pro` 现在均声明原生 `image_edit` operation。
- 未指定模型的当前本地图片编辑现在与普通生图一样默认使用 `gpt-image-2`。Skill 仍只会通过已声明 operation 发送附件，绝不自动切换模型。

## 0.13.9 — 2026-09-02

- 收紧无法传输的当前本地参考图：所选模型未声明兼容的原生 multipart 参考输入时，Skill 会在任何付费请求前停止，只接受用户提供该图的公网 HTTPS URL，或在新的明确请求中忽略附件并改为纯文生图。绝不查看、概括、复刻、上传、检索该图，也绝不把它静默转写为文生图提示词。
- 将已确认的原生媒体交付设为本轮硬结束边界：交付请求结果后只返回一次完成回执并结束；没有新的明确用户请求时，不检查成片、不搜索历史或工作区、不调用无关 Skill，也不创建新的媒体任务。

## 0.13.8 — 2026-09-01

- 在直连运行器中关闭 WorkBuddy 的标准输入绕过：所有 WorkBuddy `POST` 现在会在参数校验阶段拒绝 `--json-stdin` 和 `--multipart-stdin`，只接受有界 Base64 请求体参数。
- 将原生视频附件设为严格路由：已声明的图片、视频或音频 operation 必须使用其声明的 multipart 路径和字段，不能降级为只含文本的 JSON 视频请求。Seedance 2.0 Mini 的参考图视频必须使用 `reference_image_video` 并提交 1–9 个 `reference_images` multipart 文件；当前宿主会话无法提供附件字节时，Skill 会在计费前停止。

## 0.13.7 — 2026-09-01

- 修复 WorkBuddy 媒体提交在真正请求 API 前卡住的问题：其 Bash 链路现在将有界 JSON 请求体或 multipart 描述对象作为规范 Base64 参数传给运行器，不再依赖标准输入。运行器只在内存中解码，仍只请求同一固定且已认证的 API 路径。

## 0.13.6 — 2026-09-01

- 将媒体提交结果未知设为硬结束边界：已开始的 `POST` 没有可用宿主输出且没有已验证任务 ID 时，Skill 只返回一次回执并结束当前回复，不再调用工具、读状态、轮询或延长后续处理。后续没有任务 ID 的“继续”请求必须先明确确认要创建一次新的付费任务。

## 0.13.5 — 2026-09-01

- 将客户端下载页的 Agent 提示词收敛为一句可直接复制的官方仓库安装提示。
- 加固安装契约：新克隆的官方 `main` 是只读安装载荷。校验失败或克隆变脏时必须停止；绝不运行生成器、文档同步、格式化、修复命令、Git 写入或任何为让校验通过而改写源码的命令。
- 防止运行器在已开始 `POST` 后返回空、截断、格式异常或不可用输出时重复提交付费媒体任务。Skill 现在将此情形视为提交结果未知；仅继续已验证返回的任务 ID，绝不自动创建替代任务。
- 修复 WorkBuddy 按模型保存 Pure Tokens `/v1/...` 资源 URL 时无法解析凭据的问题。运行器仍只接受无 query、无 fragment 的固定 API origin，要求唯一无歧义凭据，并且只会请求固定允许的 API 路径。

## 0.13.4 — 2026-09-01

- 新增确定的模型访问分组恢复路径：允许的一次诊断后，如果当前认证目录仍未返回用户指定的精确图片或视频模型，Skill 会引导用户在 Pure Tokens 客户端配置中勾选包含该模型的分组，确保受管 Key 覆盖所选分组，执行“验证并应用”，然后新开宿主会话并明确重试。
- 除非认证 API 明确返回模型到分组的映射，Skill 不会猜测分组名称或声称模型属于某个分组。

## 0.13.3 — 2026-09-01

- 将受管固定端点凭据运行器扩展至所有具备已批准、可审计本地连接契约的受支持宿主：Claude Code、Codex、WorkBuddy、Gemini CLI、Grok Build、OpenCode。每个解析器只接受精确匹配的 Pure Tokens 配置，只在内存中为允许的固定 API 请求保留一个无歧义凭据，绝不展示、保存或索取凭据。
- 所有 API 专项 Skill、安装载荷契约、宿主矩阵和校验现在统一使用这六个受管运行器执行 API 身份检查、模型目录、余额、Images、Videos、同任务状态查询和原生媒体交付。
- 将 Trae 明确标记为唯一的手动凭据配置例外。其产品契约没有已批准的本地凭据解析器，因此 Skill 会安全停止，不会扫描或推断 Trae 用户状态。

## 0.13.2 — 2026-09-01

- 新增已验证的 WorkBuddy 受管直连运行器绑定。它只在内存中从 WorkBuddy 模型配置狭义匹配 `https://api.puretokensx.com/v1` 的模型凭据，只用于允许的固定 API 路径，绝不展示、保存或索取凭据。
- 明确宿主矩阵：七个宿主都可安装，但目前只有 Grok Build 和 WorkBuddy 已验证可执行带认证的直连媒体请求。其他宿主会安全停止，不再暗示通用 HTTP 请求会继承聊天模型凭据。
- 为已验证运行器补齐受限、按资料声明的 multipart 附件提交，并加固原生媒体交付：固定路径白名单、仅常规文件附件、附件数量和大小限制、内容类型检查、交付大小上限、禁止覆盖以及失败部分文件清理。
- 清除“Skill 不读取配置”与“受管运行器在内存中狭义绑定凭据”之间的矛盾描述。Skill 仍不会展示、比较或报告宿主配置。

## 0.13.1 — 2026-09-01

- 新增受管的 Grok Build 直连 API 运行器。它只在内存中狭义解析已配置的 Pure Tokens 模型凭据，只发送到允许的固定 Pure Tokens API 路径，绝不打印、保存或索取该凭据。
- `sync`、`install` 和 `upgrade` 现在会在专项 Skill 同级安装或原子升级受管 `.puretokens-runtime`；若存在非受管运行器冲突，会在改动任何 Skill 前停止。
- 移除“所有宿主都会向通用 HTTP 请求自动注入模型凭据”的错误假设。Grok Build 的 API 身份、模型目录、余额、Images、Videos、任务状态和媒体内容请求现在使用受管直连运行器，而不是通用 Fetch/WebFetch。

## 0.13.0 — 2026-09-01

- 六个可安装 Skill 的名称和目录统一为 Agent Skills 标准 kebab-case：`puretokens-balance`、`puretokens-connection`、`puretokens-models`、`puretokens-image`、`puretokens-video`、`puretokens-update`。
- `sync` 会先将已验证的旧受管 Skill 目录移到可恢复的隐藏备份，再安装当前目录；遇到非受管冲突时仍拒绝改动。
- 只声明并校验当前支持的七个宿主：Claude Code、Codex、WorkBuddy、Gemini CLI、Grok Build、OpenCode、Trae。每个宿主都通过其已记录的全局 Skill 目录手动安装源文件。
- 从当前产品说明中移除已废弃的交付机制和合并媒体 Skill。
- 为全部受支持宿主明确直连执行契约：使用已有运行时认证、请求固定完整 HTTPS URL、接收 JSON 任务响应、查询同一任务状态并交付原生媒体字节。图片和视频没有替代执行路径。

## 0.12.3 — 2026-08-31

- 图片和视频任务处理与公开媒体契约对齐：`reconciliation_required: true` 优先于生命周期状态，会停止常规轮询、保留同一任务 ID，绝不提交替代任务或推断退款。
- 状态查询遇到 HTTP `429` 时，会在本轮预算内遵循有效 `Retry-After`，且只继续查询同一任务；其他服务端、传输和超时读取仍是有界停止条件。
- 图片编辑按 transport 区分：公网 URL 编辑必须存在精确声明的 JSON `image_edit` operation，原生附件必须存在精确声明的 multipart operation。参考图字段本身不授权编辑，也不会静默降级。

## 0.12.2 — 2026-08-31

- 图片和视频失败回执统一包含阶段、可用时的 HTTP 状态、安全的用户提示、下一步和服务返回的限流等待时间。
- 明确禁止在失败输出中暴露原始错误体、上游标识、内部 URL、堆栈、请求数据、凭据和用户媒体。

## 0.12.1 — 2026-08-31

- 优化图片请求理解：区分文生图、视觉参考和编辑；输入图片角色不明确时先询问；生成简报时保留用户已声明的意图。
- 明确 `n` 只表示同一简报的变体。多个不同资产不会被静默塞入 `n`、丢弃或拆成多笔付费任务。

## 0.12.0 — 2026-08-30

- 普通文生图和文生视频使用已安装的版本化模型选择资料直接提交，不再为每个任务读取媒体目录预检。
- 实时媒体目录只在明确查询、安装资料缺口或一次拒绝后诊断时按需读取。目录读取失败不能阻止有效核心生成，也不会自动重提。
- 加强异步任务处理：只规范化已声明的顶层任务 ID；未知状态停止；保留不确定提交结果；绝不自动重提。
